import React from 'react';
import ReactDOM from 'react-dom';
import Page from './components/page';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Page ballColor={ballColor} />);
